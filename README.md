# fire-risk-prediction-onnx-lambda
Fire detection system using MODIS imagery deployed on AWS Lambda with Docker containers. Processes NDVI and LST from S3, runs an ONNX model to generate fire probability maps, and exposes an HTTP inference service via API Gateway. Fully replicable and production-ready.
